# ========================================
# CONFIGURAÇÃO - SISTEMA DE FATURAMENTO
# ========================================
# Copie este arquivo para .env e preencha com seus valores
# NUNCA commite o arquivo .env com dados reais!

# ========================================
# AMBIENTE
# ========================================
NODE_ENV=production
PORT=5001

# ========================================
# SEGURANÇA - JWT
# ========================================
# IMPORTANTE: Gere uma chave forte e única!
# Sugestão: use um gerador online ou execute: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=sua_chave_super_secreta_aqui_minimo_32_caracteres

# ========================================
# BANCO DE DADOS
# ========================================
# Em LOCAL (desenvolvimento): SQLite automático
# Em PRODUÇÃO (Railway): PostgreSQL fornecido automaticamente
# Railway cria esta variável automaticamente ao adicionar PostgreSQL
DATABASE_URL=postgresql://user:password@host:port/database

# ========================================
# EMAIL (SMTP)
# ========================================
# Para Gmail:
# 1. Habilite verificação em 2 etapas
# 2. Gere uma "Senha de App" em: https://myaccount.google.com/apppasswords
# 3. Use a senha de app gerada em EMAIL_PASS (não sua senha do Gmail!)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=seu.email@gmail.com
EMAIL_PASS=sua_senha_de_app_gmail_16_caracteres
EMAIL_FROM=noreply@sistema.com

# Para outros provedores (Outlook, etc):
# EMAIL_HOST=smtp-mail.outlook.com
# EMAIL_PORT=587

# ========================================
# URLs - CONEXÃO FRONTEND/BACKEND
# ========================================
# IMPORTANTE: Preencha após deploy no Railway
# FRONTEND_URL: usado para CORS (sem barra no final!)
# BACKEND_URL: opcional, para referência

# Exemplo em produção:
# FRONTEND_URL=https://sistema-faturamento-frontend.up.railway.app
# BACKEND_URL=https://sistema-faturamento-backend.up.railway.app

# Exemplo em desenvolvimento:
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:5001

# ========================================
# NOTAS IMPORTANTES
# ========================================
# 
# 1. NUNCA use senhas reais de email - sempre use "Senhas de App"
# 2. JWT_SECRET deve ter no mínimo 32 caracteres
# 3. FRONTEND_URL não deve ter barra "/" no final
# 4. DATABASE_URL é criada automaticamente pelo Railway
# 5. Em produção, remova valores de exemplo antes de commitar
#
